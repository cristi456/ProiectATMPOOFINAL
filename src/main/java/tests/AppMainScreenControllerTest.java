/*
 * package tests;
 * 
 * import org.junit.jupiter.api.Test;
 * 
 * import controllers.AppMainScreenController;
 * 
 * import static org.junit.jupiter.api.Assertions.*;
 * 
 * class AppMainScreenControllerTest {
 * 
 * @Test void testSelectRandomSalut() { AppMainScreenController controller = new
 * AppMainScreenController(); String salut = controller.selectRandomSalut();
 * 
 * assertNotNull(salut); assertTrue(salut.matches("^[A-Z].*")); }
 * 
 * @Test void testSetUsername() { AppMainScreenController controller = new
 * AppMainScreenController(); controller.setUsername("John");
 * 
 * assertEquals("Buna ziua, John!", controller.nameOfUser.getText()); }
 * 
 * @Test void testPinNouLabelPromptText() { AppMainScreenController controller =
 * new AppMainScreenController(); String promptText =
 * controller.pinNouLabel.getPromptText();
 * 
 * assertNotNull(promptText); assertFalse(promptText.isEmpty()); }
 * 
 * @Test void testGreetingContainsComma() { AppMainScreenController controller =
 * new AppMainScreenController(); String greeting = controller.randomGreeting();
 * 
 * assertTrue(greeting.contains(",")); }
 * 
 * @Test void testNumerarLabelPromptText() { AppMainScreenController controller
 * = new AppMainScreenController(); String promptText =
 * controller.getNumerarLabel().getPromptText();
 * 
 * assertNotNull(promptText); assertFalse(promptText.isEmpty()); } }
 */